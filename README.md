支持Debian和Ubuntu 如果服务器的防火墙不在系统内请手动在控制台开启25565端口

## 一键执行命令

```bash
wget -N --no-check-certificate https://raw.githubusercontent.com/greepar/HypForward/refs/heads/main/forward.sh && chmod +x forward.sh && sudo bash forward.sh
